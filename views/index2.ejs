<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Recipe</title>
</head>
<body>
    <%if(res.length==0){%>
        <h1>Couldn't Find any Recipes</h1>
    <%}%>
    <% for(var i=0;i<res.length;i++){ %>
        <div>
            <img src="<%=res[i].image%>">
            <h3 style="display:inline-block;"><a target="_blank" href="https://www.google.com/search?q=<%=res[i].title.replaceAll(' ','+')%>"><%=res[i].title%></a></h3>
        </div>
    <%}%>
</body>
</html>